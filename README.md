# job-watch

Checks company job boards and the SimplifyJobs new-grad tracker every 15 minutes for
early-career SWE roles open to Canada. New finds are filed as a GitHub issue (emailed to you).
Close an issue once you've handled it. Run locally with `python3 job_watch.py --all`.
